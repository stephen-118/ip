interface:
  display_name: "Test UI"
  short_description: "Run fail-fast console UI acceptance tests"
  default_prompt: "Run the console UI tests in test/ui-test-plan.md and show the test-session transcript."
